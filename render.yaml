services:
  - type: web
    name: valichord-at-home
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --workers 1 --threads 4 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
